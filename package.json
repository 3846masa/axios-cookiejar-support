{
  "private": true,
  "workspaces": [
    "workspaces/axios-cookiejar-support",
    "workspaces/tests/**"
  ],
  "scripts": {
    "build": "yarn workspace axios-cookiejar-support run build",
    "test": "yarn workspaces run test"
  }
}
